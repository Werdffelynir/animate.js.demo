
import '../../animate.js/dist/animate.bundle';

if (!Animate || !Animate.Static) {}

const {
    Clip,
    Component,
    KeyboardEventManager,
    MoveClip,
    Scene,
    SceneClass,
    StepsScene,
    Timer,
    RoxyListener,
    Roxy,
    AnimationFrame,
} = Animate;

const {
    getDocument,
    query
} = Animate.Static;

function init () {

}